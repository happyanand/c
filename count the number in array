#include <stdio.h>
int main() 
{
    int n,a[5],b;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    scanf("%d",&b);
    int c=0;
    for(int i=0;i<n;i++)
    {
        if(a[i]>2*b)
        {
           c+=1;
        }
    }
    printf("%d",c);
}
