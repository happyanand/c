#include <stdio.h>
int main() 
{
    int n,a,b;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d %d",&a,&b);
        if(a<b)
        {
            printf("%d > %d\n",b,a);
        }
        else
        {
            printf("%d > %d\n",a,b);
        }
    }
    	return 0;
}
