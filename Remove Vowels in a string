#include <stdio.h>
#include <string.h>
int main() 
{
    char a[9999];
    int l;
    scanf("%[^\n]",a);
    l=strlen(a);    
    for(int i=0;i<l;i++)
    {
        if(a[i]!='a' && a[i]!='e' && a[i]!='i' && a[i]!='o' && a[i]!='u' && a[i]!='A' && a[i]!='E' && a[i]!='I' && a[i]!='O' && a[i]!='U')
        {
            printf("%c",a[i]);
        }
    }
    return 0;
}



Sample Input 0

hello
Sample Output 0

hll
Explanation 0

The given string is "hello", here "e" and "o" are vowels, after the removal of vowels the output is, "hll"

Sample Input 1

have a nice day
Sample Output 1

hv  nc dy
Explanation 1

The string is "have a nice day". The vowels present here is "a, e, i". After the removal of all the vowels present in the given string, the output should be "hv nc dy".
