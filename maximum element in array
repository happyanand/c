#include <stdio.h>
int main() 
{
    int a[5]={2,3,4,1,6};
    int large=a[0];
    for(int i=0;i<5;i++)
    {
        if(a[i]>large)
        {
            large=a[i];
        }
    }
    printf("large %d\n",large);
	return 0;
}
