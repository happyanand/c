#include <stdio.h>
int gcd(int a,int b)
{
    int c;
    for(int i=2;i<a+b;i++)
    {
        if(a%i==0 && b%i==0)
        {
            c=i;
        }
    }
    printf("%d",c);
}
int main() 
{
    int a,b;
    scanf("%d%d",&a,&b);
    gcd(a,b);
	return 0;
}
